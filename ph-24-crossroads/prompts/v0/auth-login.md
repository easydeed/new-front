# V0 Prompt — Login Page (UI‑only)

**Change only visuals. Do NOT change handlers, fetch calls, or AuthManager usage.**
- Layout: clean, light theme, “Tech Vibrance”, no heavy gradients.
- Components: accessible inputs, large CTA button, error/success messaging.
- Keep: email/password fields, show/hide password (optional), query param prefill, redirect support.

Paste the current file contents and replace markup below the logic section.
