/**
 * PropertySearchV0.tsx
 * Paste V0 visual markup here, but DO NOT change props, onChange/onSelect/onVerified handlers, or enrichment logic.
 * Keep Google Places + SiteX behavior intact.
 */
export function PropertySearchV0(props: any) {
  // Render-only wrapper; call into your existing logic component under the hood.
  return props.children || null;
}
