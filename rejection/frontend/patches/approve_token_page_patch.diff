*** a/frontend/src/app/approve/[token]/page.tsx
--- b/frontend/src/app/approve/[token]/page.tsx
***************
*** 1,80 ****
--- 1,160 ----
+ // Patch: ensure "Request Changes" sends JSON with comments and handles success
+ 'use client';
+ import React from 'react';
+ import { useParams, useRouter } from 'next/navigation';
+ 
+ export default function ApprovePage() {
+   const params = useParams<{ token: string }>();
+   const router = useRouter();
+   const token = params?.token as string;
+ 
+   const handleApprove = async () => {
+     const res = await fetch(`/approve/${token}`, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ approved: true }) });
+     if (res.ok) {
+       alert('Approved successfully.');
+       router.push('/thank-you');
+     } else {
+       const t = await res.text();
+       alert(`Failed: ${t}`);
+     }
+   };
+ 
+   const handleRequestChanges = async () => {
+     const comments = window.prompt('Please enter feedback for the owner (why is this rejected)?');
+     const body = { approved: false, comments };
+     const res = await fetch(`/approve/${token}`, {
+       method: 'POST',
+       headers: { 'Content-Type': 'application/json' },
+       body: JSON.stringify(body)
+     });
+     if (res.ok) {
+       alert('Your feedback has been submitted. The owner has been notified.');
+       // optional redirect
+       window.location.href = '/';
+     } else {
+       const t = await res.text();
+       alert(`Failed to submit feedback: ${t}`);
+     }
+   };
+ 
+   return (
+     <div className="max-w-xl mx-auto p-6">
+       <h1 className="text-xl font-semibold mb-4">Review Shared Deed</h1>
+       <div className="flex gap-3">
+         <button onClick={handleApprove} className="px-4 py-2 rounded-md bg-green-600 text-white">✅ Approve</button>
+         <button onClick={handleRequestChanges} className="px-4 py-2 rounded-md bg-red-600 text-white">❌ Request Changes</button>
+       </div>
+     </div>
+   );
+ }
