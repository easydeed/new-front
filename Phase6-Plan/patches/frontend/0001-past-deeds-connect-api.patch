*** Begin Patch
*** Update File: frontend/src/app/past-deeds/page.tsx
@@
-const [deeds, setDeeds] = useState([
-  {
-    id: 1,
-    property: '123 Main St, Los Angeles, CA 90210',
-    deedType: 'Grant Deed',
-    status: 'completed',
-    createdDate: '2024-01-15',
-    lastUpdated: '2024-01-16',
-    progress: 100
-  },
-  // ... more hardcoded data
-]);
+type DeedSummary = {
+  id: string;
+  property?: string;
+  deed_type?: string;
+  status?: 'draft'|'completed'|'in_progress';
+  created_at?: string;
+  updated_at?: string;
+  pdf_url?: string;
+};
+const [deeds, setDeeds] = useState<DeedSummary[]>([]);
+const [loading, setLoading] = useState<boolean>(true);
+const [error, setError] = useState<string | null>(null);
+
+useEffect(() => {
+  const api = process.env.NEXT_PUBLIC_API_URL || '/api';
+  const token = (typeof window !== 'undefined') ? localStorage.getItem('token') : null;
+  (async () => {
+    try {
+      setLoading(true);
+      const res = await fetch(`${api}/deeds`, {
+        headers: token ? { 'Authorization': `Bearer ${token}` } : {}
+      });
+      if (!res.ok) throw new Error(`HTTP ${res.status}`);
+      const data = await res.json();
+      setDeeds(Array.isArray(data.deeds) ? data.deeds : data);
+    } catch (e:any) {
+      setError(e.message || 'Failed to load deeds');
+    } finally {
+      setLoading(false);
+    }
+  })();
+}, []);
@@
-  {deeds.map(d => (
+  {loading && <div className="text-slate">Loading deeds…</div>}
+  {error && <div className="text-red-600">Error: {error}</div>}
+  {!loading && !error && deeds.map(d => (
     <tr key={d.id}>
-      <td>{d.property}</td>
-      <td>{d.deedType}</td>
-      <td>{d.status}</td>
-      <td>{d.createdDate}</td>
-      <td>{d.lastUpdated}</td>
-      <td>{d.progress}%</td>
+      <td>{d.property || '—'}</td>
+      <td>{d.deed_type || 'Grant Deed'}</td>
+      <td>{d.status || '—'}</td>
+      <td>{d.created_at?.slice(0,10) || '—'}</td>
+      <td>{d.updated_at?.slice(0,10) || '—'}</td>
+      <td>
+        {d.status === 'draft' ? (
+          <a className="text-teal underline" href={`/create-deed?resume=${d.id}`}>Continue Draft</a>
+        ) : d.pdf_url ? (
+          <a className="text-teal underline" href={d.pdf_url} target="_blank" rel="noreferrer">Download PDF</a>
+        ) : '—'}
+      </td>
     </tr>
   ))}
*** End Patch
