*** Begin Patch
*** Update File: backend/main.py
@@
-from fastapi import FastAPI
+from fastapi import FastAPI
@@
 app = FastAPI()
@@
+# Mount extra deed types router
+try:
+    from routers.deeds_extra import router as deeds_extra_router
+    app.include_router(deeds_extra_router)
+except Exception as e:
+    import logging
+    logging.getLogger(__name__).warning("deeds_extra router not mounted: %s", e)
*** End Patch
