*** Begin Patch
*** Update File: frontend/src/features/wizard/mode/components/PropertyStepBridge.tsx
@@
-import dynamic from 'next/dynamic';
-import React from 'react';
-import { useWizardStoreBridge } from '../bridge/useWizardStoreBridge';
+import dynamic from 'next/dynamic';
+import React from 'react';
+import { useWizardStoreBridge } from '../bridge/useWizardStoreBridge';
+
+/**
+ * Patch 6-c: Keep the existing property verification (Classic Step 1)
+ * but render it as the first screen of Modern. On verify, call markVerified
+ * and move forward in ModernEngine.
+ *
+ * We load maps client-side only to avoid SSR/hydration issues and
+ * provide a fallback if legacy Places APIs are not available.
+ */
 
 const PropertySearchWithTitlePoint = dynamic(
   () => import('@/components/PropertySearchWithTitlePoint'),
   { ssr: false }
 );
 
 export default function PropertyStepBridge({ onVerified }: { onVerified: (v: any) => void }) {
   const { markVerified } = useWizardStoreBridge();
   return (
     <div className="modern-qna">
       <h1 className="modern-qna__title">Let’s confirm the property</h1>
       <p className="modern-qna__why">We’ll prefill APN, county, and owner if available.</p>
       <PropertySearchWithTitlePoint
         onVerified={(data: any) => {
           markVerified(data);
           onVerified(data);
         }}
         // Safe feature-detection fallback for new Places APIs
         googlePlacesOverrides={{
           preferNewAPIs: true
         }}
       />
     </div>
   );
 }
*** End Patch
