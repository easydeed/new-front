*** Begin Patch
*** Update File: frontend/src/features/wizard/mode/components/ModeToggle.tsx
@@
-import React from 'react';
-import { useWizardMode } from '../../mode/ModeContext';
+import React from 'react';
+import { useWizardMode } from '../../mode/ModeContext';
 
 export default function ModeToggle() {
   const { mode, setMode, hydrated } = useWizardMode();
   if (!hydrated) return null;
   return (
     <div className="mode-toggle">
       <label className="switch">
         <input
           type="checkbox"
           checked={mode === 'modern'}
           onChange={(e) => {
             const next = e.target.checked ? 'modern' : 'classic';
             setMode(next);
             try { localStorage.setItem('wizard_mode', next); } catch {}
             const url = new URL(window.location.href);
             url.searchParams.set('mode', next);
             window.location.replace(url.toString());
           }}
         />
         <span className="slider" />
       </label>
       <span className="mode-toggle__label">{mode === 'modern' ? 'Modern' : 'Classic'}</span>
     </div>
   );
 }
*** End Patch
