# ENGINE1 mockups — reference, not copy

These four files supersede ENGINE1's brief. They are owner mockups
committed as reference implementations. The rationale is in the copy.
Read them before building.

- `homepage.html` — two-door fork above the fold (platform / escrow-and-title); 422 sample as the platform door's proof
- `developers.html` — confirmation-first docs, instrument doctrine, v1 deed-family boundary
- `confirmation.html` — the state machine, confirm call, auditor artifact
- `trust.html` — the honesty gradient the other three pages should match

**Nothing from these pages ships as present-tense product copy until
the owner decides which claims to build and which to cut.** Several
claims are not currently true. The inventory and the two product
rulings (`draft_sha256` on confirm, `reviewer.license` as required)
are in `docs/OWNER_LEDGER.md` and the ticket report. The banned-claims
gate should cover any claim that stays.
