# V0 PROMPT — Form Inputs (labels/help/errors)

Provide a consistent pattern for label, input, help text, and error messages.
- No logic changes; just markup/classes
- Ensure `aria-invalid`, `aria-describedby` set by existing logic remain effective
