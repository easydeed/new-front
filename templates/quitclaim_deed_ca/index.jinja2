<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Quitclaim Deed - California</title>
    <style>
        /* ==========================================================================
           QUITCLAIM DEED - CALIFORNIA
           Professional template designed for county recorder acceptance
           Optimized for PDFShift (Chrome headless) rendering
           
           Uses same design system as Grant Deed for consistency
           ========================================================================== */

        @page {
            size: letter;
            margin: 0.5in 0.5in 0.75in 1in;  /* Top Right Bottom Left - 1in left for binding */
        }

        @media print {
            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Times New Roman", Times, serif;
            font-size: 12pt;
            line-height: 1.4;
            color: #000;
            background: #fff;
        }

        .deed-page {
            width: 100%;
            min-height: 100%;
        }

        /* Header Section */
        .header-grid {
            display: grid;
            grid-template-columns: 1fr 3.5in;
            gap: 0.25in;
            margin-bottom: 0.15in;
            min-height: 2in;
        }

        .recording-info {
            font-size: 10pt;
            line-height: 1.35;
        }

        .recording-info-label {
            font-weight: bold;
            text-transform: uppercase;
            font-size: 9pt;
            letter-spacing: 0.5px;
            margin-bottom: 2px;
        }

        .recording-info-value {
            margin-bottom: 0.12in;
        }

        /* Right column container for recorder box + reference numbers */
        .right-column {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }

        .recorder-space {
            width: 3.5in;
            border: 2px solid #000;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding-top: 0.15in;
            min-height: 2.5in;
        }

        .recorder-space-label {
            font-size: 8pt;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #000;
            text-align: center;
            font-weight: bold;
        }

        /* Reference Numbers - below recorder box */
        .reference-numbers {
            margin-top: 0.12in;
            text-align: right;
            font-size: 10pt;
        }

        .ref-line {
            margin-bottom: 0.05in;
            display: flex;
            justify-content: flex-end;
            align-items: baseline;
            gap: 0.08in;
        }

        .ref-label {
            font-weight: normal;
            font-size: 10pt;
        }

        .ref-value {
            display: inline-block;
            min-width: 1.5in;
            border-bottom: 1pt solid #000;
            text-align: left;
            padding-left: 0.05in;
            font-size: 10pt;
        }

        .ref-value.monospace {
            font-family: "Courier New", Courier, monospace;
            letter-spacing: 0.5px;
        }

        /* Horizontal separator line */
        .separator-line {
            border-bottom: 1px solid #000;
            margin-bottom: 0.1in;
            padding-bottom: 0.08in;
        }

        /* Document Title */
        .deed-title {
            text-align: center;
            font-size: 22pt;
            font-weight: bold;
            letter-spacing: 4px;
            text-transform: uppercase;
            margin: 0.25in 0;
            padding: 0.12in 0;
        }

        /* DTT Section */
        .dtt-section {
            margin: 0.2in 0;
            padding: 0.15in;
            border: 1.5px solid #000;
            font-size: 10pt;
            line-height: 1.5;
            page-break-inside: avoid;
        }

        .dtt-header {
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 0.1in;
        }

        .dtt-amount {
            font-size: 11pt;
            margin-bottom: 0.08in;
        }

        .dtt-amount-value {
            font-weight: bold;
            text-decoration: underline;
        }

        .dtt-options { margin-left: 0.2in; }

        .dtt-option {
            margin: 0.05in 0;
            display: flex;
            align-items: flex-start;
            gap: 0.1in;
        }

        .checkbox {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 14px;
            height: 14px;
            border: 1.5px solid #000;
            font-size: 10pt;
            font-weight: bold;
            flex-shrink: 0;
            margin-top: 1px;
        }

        .checkbox.checked {
            background: #000;
            color: #fff;
        }

        .dtt-area-line {
            margin-top: 0.08in;
            display: flex;
            align-items: center;
            gap: 0.3in;
        }

        .city-name-field {
            border-bottom: 1px solid #000;
            min-width: 2in;
            display: inline-block;
            padding: 0 0.1in;
        }

        /* Deed Body */
        .deed-body {
            margin: 0.2in 0;
            text-align: justify;
            font-size: 12pt;
            line-height: 1.5;
        }

        .deed-body p { margin-bottom: 0.15in; }

        .party-name {
            font-weight: bold;
            text-transform: uppercase;
        }

        .party-designation { font-style: normal; }

        .grant-language {
            text-transform: uppercase;
            font-weight: bold;
        }

        .county-name {
            font-weight: bold;
            text-transform: uppercase;
            border-bottom: 1px solid #000;
            padding: 0 0.1in;
        }

        /* Legal Description */
        .legal-description-section {
            margin: 0.2in 0;
            page-break-inside: avoid;
        }

        .legal-description-content {
            font-size: 11pt;
            line-height: 1.5;
            text-align: justify;
            white-space: pre-wrap;
            margin-top: 0.1in;
        }

        .legal-description-exhibit-ref {
            font-weight: bold;
            font-style: italic;
        }

        .apn-line {
            margin-top: 0.15in;
            font-size: 11pt;
        }

        .apn-label { font-weight: bold; }

        .apn-value {
            font-family: "Courier New", Courier, monospace;
            letter-spacing: 1px;
        }

        /* Execution Section */
        .execution-section {
            margin-top: 0.35in;
            page-break-inside: avoid;
        }

        .execution-date {
            margin-bottom: 0.25in;
            font-size: 12pt;
        }

        .date-label { font-weight: bold; }

        .date-value {
            border-bottom: 1px solid #000;
            padding: 0 0.15in;
            min-width: 2.5in;
            display: inline-block;
        }

        .signature-block { margin-top: 0.4in; }
        .signature-entry { margin-bottom: 0.35in; }

        .signature-line {
            border-bottom: 1px solid #000;
            width: 3.5in;
            height: 0.35in;
        }

        .signature-name {
            font-size: 11pt;
            margin-top: 0.05in;
            text-transform: uppercase;
        }

        /* Notary Section */
        .notary-section {
            margin-top: 0.4in;
            page-break-inside: avoid;
            border-top: 2px solid #000;
            padding-top: 0.2in;
        }

        .notary-disclaimer {
            font-size: 9pt;
            font-style: italic;
            text-align: justify;
            line-height: 1.4;
            margin-bottom: 0.2in;
            padding: 0.12in;
            border: 1px solid #000;
        }

        .notary-title {
            text-align: center;
            font-weight: bold;
            font-size: 11pt;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 0.15in;
        }

        .notary-venue {
            font-size: 11pt;
            margin-bottom: 0.15in;
            line-height: 1.6;
        }

        .venue-line {
            display: flex;
            align-items: center;
        }

        .venue-state { width: 2.5in; }
        .venue-bracket { font-size: 14pt; }

        .venue-ss {
            margin-left: 0.3in;
            font-size: 10pt;
        }

        .county-field {
            border-bottom: 1px solid #000;
            min-width: 2in;
            display: inline-block;
            padding: 0 0.1in;
        }

        .notary-body {
            font-size: 11pt;
            line-height: 1.6;
            text-align: justify;
            margin: 0.15in 0;
        }

        .notary-body p { margin-bottom: 0.12in; }

        .notary-field {
            border-bottom: 1px solid #000;
            min-width: 1.5in;
            display: inline-block;
            padding: 0 0.05in;
        }

        .notary-field.long { min-width: 2.5in; }

        .notary-certification {
            margin-top: 0.15in;
            font-size: 11pt;
            line-height: 1.5;
        }

        .notary-witness {
            margin-top: 0.2in;
            font-weight: bold;
            font-size: 11pt;
        }

        .notary-execution {
            display: grid;
            grid-template-columns: 1fr 2.25in;
            gap: 0.5in;
            margin-top: 0.25in;
            align-items: end;
        }

        .notary-signature-block { margin-top: 0.1in; }

        .notary-signature-line {
            border-bottom: 1px solid #000;
            width: 3in;
            height: 0.3in;
        }

        .notary-signature-label {
            font-size: 10pt;
            margin-top: 0.05in;
        }

        .notary-print-name {
            margin-top: 0.15in;
            font-size: 10pt;
        }

        .notary-commission {
            margin-top: 0.15in;
            font-size: 10pt;
        }

        .commission-field {
            border-bottom: 1px solid #000;
            min-width: 1.5in;
            display: inline-block;
        }

        .notary-seal-area {
            border: 2px solid #000;
            width: 2.25in;
            height: 2.25in;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .seal-label {
            font-size: 10pt;
            font-weight: bold;
            text-transform: uppercase;
            color: #666;
        }

        /* Exhibit Page */
        .page-break { page-break-before: always; }

        .exhibit-page { padding-top: 0.5in; }

        .exhibit-header {
            text-align: center;
            margin-bottom: 0.4in;
        }

        .exhibit-title {
            font-size: 16pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .exhibit-subtitle {
            font-size: 12pt;
            margin-top: 0.1in;
            font-style: italic;
        }

        .exhibit-reference {
            font-size: 10pt;
            margin-top: 0.15in;
        }

        .exhibit-content {
            font-size: 11pt;
            line-height: 1.6;
            text-align: justify;
            white-space: pre-wrap;
        }

        .exhibit-apn {
            margin-top: 0.3in;
            font-size: 10pt;
            border-top: 1px solid #ccc;
            padding-top: 0.15in;
        }

    </style>
</head>
<body>
    <div class="deed-page">

        <!-- HEADER -->
        <div class="header-grid">
            <!-- Left Column: Recording Information -->
            <div class="recording-info">
                <div class="recording-info-label">Recording Requested By:</div>
                <div class="recording-info-value">
                    {{ requested_by or title_company or '' }}
                </div>

                <div class="recording-info-label">When Recorded Mail To:</div>
                <div class="recording-info-value">
                    {% if return_to %}
                        {% if return_to.get('name') %}{{ return_to.get('name') }}<br>{% endif %}
                        {% if return_to.get('company') %}{{ return_to.get('company') }}<br>{% endif %}
                        {% if return_to.get('address1') %}{{ return_to.get('address1') }}<br>{% endif %}
                        {% if return_to.get('address2') %}{{ return_to.get('address2') }}<br>{% endif %}
                        {% set _city = return_to.get('city', '') %}
                        {% set _state = return_to.get('state', '') %}
                        {% set _zip = return_to.get('zip', '') %}
                        {% if _city or _state or _zip %}
                            {{ _city }}{% if _city and (_state or _zip) %}, {% endif %}{{ _state }} {{ _zip }}
                        {% endif %}
                    {% else %}
                        _________________________________<br>
                        _________________________________<br>
                        _________________________________
                    {% endif %}
                </div>

                <div class="recording-info-label">Mail Tax Statements To:</div>
                <div class="recording-info-value" style="margin-bottom: 0;">
                    {% if mail_tax_to %}{{ mail_tax_to }}{% else %}Same as above{% endif %}
                </div>
            </div>

            <!-- Right Column: Recorder Box + Reference Numbers -->
            <div class="right-column">
                <div class="recorder-space">
                    <div class="recorder-space-label">
                        Space Above This Line<br>
                        For Recorder's Use Only
                    </div>
                </div>
                
                <!-- Reference Numbers - Right aligned, below recorder box -->
                <div class="reference-numbers">
                    {% if title_order_no %}
                    <div class="ref-line">
                        <span class="ref-label">Order No.:</span>
                        <span class="ref-value">{{ title_order_no }}</span>
                    </div>
                    {% endif %}
                    {% if escrow_no %}
                    <div class="ref-line">
                        <span class="ref-label">Escrow No.:</span>
                        <span class="ref-value">{{ escrow_no }}</span>
                    </div>
                    {% endif %}
                    {% if apn %}
                    <div class="ref-line">
                        <span class="ref-label">APN:</span>
                        <span class="ref-value monospace">{{ apn }}</span>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Horizontal separator line -->
        <div class="separator-line"></div>

        <!-- DOCUMENT TITLE -->
        <h1 class="deed-title">Quitclaim Deed</h1>

        <!-- DOCUMENTARY TRANSFER TAX -->
        <div class="dtt-section">
            <div class="dtt-header">The Undersigned Grantor(s) Declare(s):</div>
            
            <div class="dtt-amount">
                Documentary Transfer Tax is 
                <span class="dtt-amount-value">${{ dtt.get('amount', '0.00') if dtt else '0.00' }}</span>
            </div>

            <div class="dtt-options">
                <div class="dtt-option">
                    <span class="checkbox {% if not dtt or dtt.get('basis') == 'full' %}checked{% endif %}">
                        {% if not dtt or dtt.get('basis') == 'full' %}✓{% endif %}
                    </span>
                    <span>Computed on full value of property conveyed, OR</span>
                </div>
                <div class="dtt-option">
                    <span class="checkbox {% if dtt and dtt.get('basis') == 'less_liens' %}checked{% endif %}">
                        {% if dtt and dtt.get('basis') == 'less_liens' %}✓{% endif %}
                    </span>
                    <span>Computed on full value less liens and encumbrances remaining at time of sale</span>
                </div>
            </div>

            <div class="dtt-area-line">
                <div class="dtt-option">
                    <span class="checkbox {% if not dtt or dtt.get('area_type') == 'unincorporated' %}checked{% endif %}">
                        {% if not dtt or dtt.get('area_type') == 'unincorporated' %}✓{% endif %}
                    </span>
                    <span>Unincorporated Area</span>
                </div>
                <div class="dtt-option">
                    <span class="checkbox {% if dtt and dtt.get('area_type') == 'city' %}checked{% endif %}">
                        {% if dtt and dtt.get('area_type') == 'city' %}✓{% endif %}
                    </span>
                    <span>City of <span class="city-name-field">{{ dtt.get('city_name', '') if dtt else '' }}</span></span>
                </div>
            </div>
        </div>

        <!-- DEED BODY -->
        <div class="deed-body">
            <p>
                FOR VALUABLE CONSIDERATION, receipt of which is hereby acknowledged,
            </p>

            <p>
                <span class="party-name">{{ grantors_text or '_______________________________________' }}</span>
                <span class="party-designation">("Grantor")</span>
            </p>

            <p>
                hereby <span class="grant-language">REMISE(S), RELEASE(S) AND QUITCLAIM(S)</span> to
            </p>

            <p>
                <span class="party-name">{{ grantees_text or '_______________________________________' }}</span>
                <span class="party-designation">("Grantee")</span>
            </p>

            <p>
                all right, title and interest in and to the following described real property 
                in the County of <span class="county-name">{{ county or '_______________' }}</span>, 
                State of California:
            </p>
        </div>

        <!-- LEGAL DESCRIPTION -->
        <div class="legal-description-section">
            {% if legal_description and legal_description|length > (exhibit_threshold or 600) %}
                <p class="legal-description-exhibit-ref">
                    See Exhibit "A" attached hereto and incorporated herein by this reference.
                </p>
            {% else %}
                <div class="legal-description-content">{{ legal_description or 'LEGAL DESCRIPTION TO BE INSERTED' }}</div>
            {% endif %}

            {% if apn %}
                <div class="apn-line">
                    <span class="apn-label">Assessor's Parcel Number:</span>
                    <span class="apn-value">{{ apn }}</span>
                </div>
            {% endif %}
        </div>

        <!-- EXECUTION -->
        <div class="execution-section">
            <div class="execution-date">
                <span class="date-label">Dated:</span>
                <span class="date-value">{{ execution_date or now().strftime('%B %d, %Y') }}</span>
            </div>

            <div class="signature-block">
                {% set signers = grantors_text.split(';') if grantors_text and ';' in grantors_text else [grantors_text] if grantors_text else [] %}
                
                {% for signer in signers %}
                    {% set signer_name = signer.strip() if signer else '' %}
                    {% if signer_name %}
                        <div class="signature-entry">
                            <div class="signature-line"></div>
                            <div class="signature-name">{{ signer_name }}</div>
                        </div>
                    {% endif %}
                {% endfor %}

                {% if not signers or signers|length == 0 %}
                    <div class="signature-entry">
                        <div class="signature-line"></div>
                        <div class="signature-name">Grantor</div>
                    </div>
                {% endif %}
            </div>
        </div>

        <!-- NOTARY ACKNOWLEDGMENT -->
        <div class="notary-section">
            <div class="notary-disclaimer">
                A notary public or other officer completing this certificate verifies only the identity 
                of the individual who signed the document to which this certificate is attached, and not 
                the truthfulness, accuracy, or validity of that document.
            </div>

            <div class="notary-title">Acknowledgment</div>

            <div class="notary-venue">
                <div class="venue-line">
                    <span class="venue-state">State of California</span>
                    <span class="venue-bracket">)</span>
                </div>
                <div class="venue-line">
                    <span class="venue-state"></span>
                    <span class="venue-bracket">)</span>
                    <span class="venue-ss">ss.</span>
                </div>
                <div class="venue-line">
                    <span class="venue-state">County of <span class="county-field">{{ county or '' }}</span></span>
                    <span class="venue-bracket">)</span>
                </div>
            </div>

            <div class="notary-body">
                <p>
                    On <span class="notary-field">____________________</span> before me, 
                    <span class="notary-field long">___________________________________</span>, 
                    a Notary Public, personally appeared 
                    <span class="notary-field long">{{ grantors_text or '___________________________________' }}</span>, 
                    who proved to me on the basis of satisfactory evidence to be the person(s) whose name(s) 
                    is/are subscribed to the within instrument and acknowledged to me that he/she/they executed 
                    the same in his/her/their authorized capacity(ies), and that by his/her/their signature(s) 
                    on the instrument the person(s), or the entity upon behalf of which the person(s) acted, 
                    executed the instrument.
                </p>
            </div>

            <div class="notary-certification">
                I certify under PENALTY OF PERJURY under the laws of the State of California 
                that the foregoing paragraph is true and correct.
            </div>

            <div class="notary-witness">
                WITNESS my hand and official seal.
            </div>

            <div class="notary-execution">
                <div class="notary-signature-block">
                    <div class="notary-signature-line"></div>
                    <div class="notary-signature-label">Signature of Notary Public</div>
                    
                    <div class="notary-print-name">
                        Name (Printed): <span class="notary-field">_________________________</span>
                    </div>
                    
                    <div class="notary-commission">
                        My Commission Expires: <span class="commission-field">_______________</span>
                    </div>
                </div>

                <div class="notary-seal-area">
                    <span class="seal-label">Place Notary Seal<br>Above</span>
                </div>
            </div>
        </div>

    </div>

    <!-- EXHIBIT A -->
    {% if legal_description and legal_description|length > (exhibit_threshold or 600) %}
    <div class="page-break"></div>
    <div class="exhibit-page">
        <div class="exhibit-header">
            <div class="exhibit-title">Exhibit "A"</div>
            <div class="exhibit-subtitle">Legal Description</div>
            <div class="exhibit-reference">
                (Attached to and made a part of Quitclaim Deed dated {{ execution_date or now().strftime('%B %d, %Y') }})
            </div>
        </div>

        <div class="exhibit-content">{{ legal_description }}</div>

        {% if apn %}
            <div class="exhibit-apn">
                <span class="apn-label">Assessor's Parcel Number:</span>
                <span class="apn-value">{{ apn }}</span>
            </div>
        {% endif %}
    </div>
    {% endif %}

    {# ================================================================
       PAGE 2 (or 3): CALIFORNIA ALL-PURPOSE ACKNOWLEDGMENT
       California Civil Code § 1189
       ================================================================ #}
    {% set document_title = 'Quitclaim Deed' %}
    {% include '_partials/notary_acknowledgment.jinja2' %}

</body>
</html>
