<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Grant Deed - California</title>
    <style>
        /* ==========================================================================
           GRANT DEED - CALIFORNIA
           DeedPro Professional Template v2.0
           
           Features:
           - CA County Recorder compliant margins
           - Title Order & Escrow number support
           - QR code verification footer
           - Perfect typography hierarchy
           - Print-optimized layout
           ========================================================================== */

        /* --------------------------------------------------------------------------
           PAGE SETUP
           California Recording Requirements:
           - Top: Space for recorder stamps (2.5" minimum clear)
           - Left: 1" for binding
           - Right: 0.5" minimum
           - Bottom: 0.5" minimum (we use 0.75" for QR footer)
           -------------------------------------------------------------------------- */
        
        @page {
            size: letter;
            margin: 0.5in 0.5in 0.75in 1in;
        }

        @media print {
            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
        }

        /* --------------------------------------------------------------------------
           BASE STYLES
           -------------------------------------------------------------------------- */

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Times New Roman", Times, Georgia, serif;
            font-size: 12pt;
            line-height: 1.5;
            color: #000;
            background: #fff;
        }

        .deed-page {
            width: 100%;
            min-height: 100%;
            position: relative;
            padding-bottom: 1.25in; /* Space for footer */
        }

        /* --------------------------------------------------------------------------
           HEADER SECTION - Recording Info & Recorder Space
           Recorder space must be minimum 3.5" × 2" per CA requirements
           -------------------------------------------------------------------------- */

        .header-section {
            display: grid;
            grid-template-columns: 1fr 3.5in;
            gap: 0.25in;
            margin-bottom: 0.2in;
            min-height: 2.5in;
        }

        .recording-info {
            font-size: 10pt;
            line-height: 1.4;
        }

        .recording-label {
            font-weight: bold;
            text-transform: uppercase;
            font-size: 8pt;
            letter-spacing: 0.5px;
            color: #000;
            margin-bottom: 2px;
        }

        .recording-value {
            margin-bottom: 0.15in;
            min-height: 0.5in;
        }

        .recorder-box {
            width: 3.5in;
            height: 2.5in;
            border: 2pt solid #000;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding-top: 0.25in;
            background: #fff;
        }

        .recorder-label {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 8pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #000;
            text-align: center;
            line-height: 1.6;
        }

        /* --------------------------------------------------------------------------
           REFERENCE LINE - APN, Title Order, Escrow
           -------------------------------------------------------------------------- */

        .reference-section {
            border-bottom: 1.5pt solid #000;
            padding: 0.08in 0;
            margin-bottom: 0.15in;
        }

        .reference-row {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            flex-wrap: wrap;
            gap: 0.25in;
            font-size: 10pt;
        }

        .reference-primary {
            display: flex;
            align-items: baseline;
            gap: 0.08in;
        }

        .reference-secondary {
            display: flex;
            gap: 0.35in;
            flex-wrap: wrap;
        }

        .ref-item {
            display: flex;
            align-items: baseline;
            gap: 0.06in;
            white-space: nowrap;
        }

        .ref-label {
            font-weight: bold;
            text-transform: uppercase;
            font-size: 9pt;
            letter-spacing: 0.3px;
        }

        .ref-value {
            font-weight: normal;
        }

        .ref-value.monospace {
            font-family: "Courier New", Courier, monospace;
            letter-spacing: 0.5px;
        }

        /* --------------------------------------------------------------------------
           DOCUMENT TITLE
           -------------------------------------------------------------------------- */

        .deed-title {
            text-align: center;
            font-size: 22pt;
            font-weight: bold;
            letter-spacing: 4px;
            text-transform: uppercase;
            margin: 0.2in 0 0.25in 0;
            padding: 0.1in 0;
        }

        /* --------------------------------------------------------------------------
           DOCUMENTARY TRANSFER TAX SECTION
           -------------------------------------------------------------------------- */

        .dtt-section {
            margin: 0.2in 0;
            padding: 0.15in 0.2in;
            border: 1.5pt solid #000;
            font-size: 10pt;
            line-height: 1.5;
            page-break-inside: avoid;
            background: #fafafa;
        }

        .dtt-header {
            font-weight: bold;
            text-transform: uppercase;
            font-size: 10pt;
            letter-spacing: 0.5px;
            margin-bottom: 0.1in;
            padding-bottom: 0.05in;
            border-bottom: 0.5pt solid #ccc;
        }

        .dtt-amount-line {
            font-size: 11pt;
            margin: 0.1in 0;
        }

        .dtt-amount-value {
            font-weight: bold;
            border-bottom: 1pt solid #000;
            padding: 0 0.15in;
            min-width: 1.5in;
            display: inline-block;
            text-align: right;
        }

        .dtt-options {
            margin: 0.1in 0 0.1in 0.15in;
        }

        .dtt-option {
            display: flex;
            align-items: flex-start;
            gap: 0.1in;
            margin: 0.06in 0;
        }

        .checkbox {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 12px;
            height: 12px;
            border: 1.5pt solid #000;
            font-size: 9pt;
            font-weight: bold;
            flex-shrink: 0;
            margin-top: 2px;
            background: #fff;
        }

        .checkbox.checked {
            background: #000;
            color: #fff;
        }

        .dtt-area-row {
            display: flex;
            gap: 0.4in;
            margin-top: 0.1in;
            padding-top: 0.08in;
            border-top: 0.5pt solid #ddd;
        }

        .city-field {
            border-bottom: 1pt solid #000;
            min-width: 2in;
            display: inline-block;
            padding: 0 0.1in;
        }

        /* --------------------------------------------------------------------------
           DEED BODY - Granting Clause
           -------------------------------------------------------------------------- */

        .deed-body {
            margin: 0.2in 0;
            text-align: justify;
            font-size: 12pt;
            line-height: 1.5;
        }

        .deed-body p {
            margin-bottom: 0.12in;
        }

        .party-block {
            margin: 0.15in 0;
        }

        .party-name {
            font-weight: bold;
            text-transform: uppercase;
        }

        .party-label {
            font-style: normal;
            font-size: 11pt;
        }

        .grant-verb {
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 0.5px;
        }

        .county-name {
            font-weight: bold;
            text-transform: uppercase;
        }

        /* --------------------------------------------------------------------------
           LEGAL DESCRIPTION
           -------------------------------------------------------------------------- */

        .legal-section {
            margin: 0.2in 0;
            page-break-inside: avoid;
        }

        .legal-content {
            font-size: 11pt;
            line-height: 1.6;
            text-align: justify;
            white-space: pre-wrap;
            margin: 0.1in 0;
            padding: 0.1in;
            background: #fafafa;
            border-left: 2pt solid #ddd;
        }

        .legal-exhibit-ref {
            font-weight: bold;
            font-style: italic;
        }

        .apn-line {
            margin-top: 0.15in;
            font-size: 11pt;
        }

        .apn-label {
            font-weight: bold;
        }

        .apn-value {
            font-family: "Courier New", Courier, monospace;
            letter-spacing: 1px;
            font-weight: bold;
        }

        /* --------------------------------------------------------------------------
           EXECUTION SECTION - Date & Signatures
           -------------------------------------------------------------------------- */

        .execution-section {
            margin-top: 0.3in;
            page-break-inside: avoid;
        }

        .execution-date {
            margin-bottom: 0.35in;
            font-size: 12pt;
        }

        .date-label {
            font-weight: bold;
        }

        .date-value {
            border-bottom: 1pt solid #000;
            padding: 0 0.15in;
            min-width: 2.5in;
            display: inline-block;
        }

        .signatures {
            margin-top: 0.3in;
        }

        .signature-entry {
            margin-bottom: 0.45in;
        }

        .signature-line {
            border-bottom: 1pt solid #000;
            width: 3.5in;
            height: 0.4in;
        }

        .signature-name {
            font-size: 11pt;
            margin-top: 0.05in;
            text-transform: uppercase;
            font-weight: 500;
        }

        .signature-capacity {
            font-size: 10pt;
            color: #333;
            font-style: italic;
        }

        /* --------------------------------------------------------------------------
           QR VERIFICATION FOOTER
           -------------------------------------------------------------------------- */

        .deed-footer {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding-top: 0.1in;
            border-top: 0.5pt solid #ccc;
        }

        .verification-row {
            display: flex;
            align-items: center;
            gap: 0.15in;
        }

        .qr-code {
            width: 0.7in;
            height: 0.7in;
            flex-shrink: 0;
            border: 0.5pt solid #ddd;
            padding: 2px;
        }

        .qr-code img {
            width: 100%;
            height: 100%;
        }

        .verification-text {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 8pt;
            color: #444;
            line-height: 1.4;
        }

        .verify-label {
            font-weight: bold;
            color: #000;
            font-size: 8pt;
        }

        .verify-url {
            font-family: "Courier New", monospace;
            font-size: 9pt;
            color: #7C4DFF;
            letter-spacing: 0.3px;
        }

        .verify-meta {
            font-size: 7pt;
            color: #888;
            margin-top: 2px;
        }

        .deedpro-brand {
            color: #7C4DFF;
            font-weight: bold;
        }

        /* --------------------------------------------------------------------------
           PAGE BREAK & EXHIBIT
           -------------------------------------------------------------------------- */

        .page-break {
            page-break-before: always;
        }

        .exhibit-page {
            padding-top: 0.5in;
        }

        .exhibit-header {
            text-align: center;
            margin-bottom: 0.4in;
        }

        .exhibit-title {
            font-size: 16pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .exhibit-subtitle {
            font-size: 12pt;
            margin-top: 0.1in;
            font-style: italic;
        }

        .exhibit-reference {
            font-size: 10pt;
            margin-top: 0.15in;
            color: #444;
        }

        .exhibit-content {
            font-size: 11pt;
            line-height: 1.6;
            text-align: justify;
            white-space: pre-wrap;
        }

        .exhibit-apn {
            margin-top: 0.3in;
            font-size: 10pt;
            border-top: 1pt solid #ccc;
            padding-top: 0.15in;
        }

    </style>
</head>
<body>
    <div class="deed-page">

        <!-- ================================================================
             HEADER - Recording Information & Recorder Space
             ================================================================ -->

        <header class="header-section">
            <div class="recording-info">
                <div class="recording-label">Recording Requested By:</div>
                <div class="recording-value">
                    {{ requested_by or title_company or '' }}
                </div>

                <div class="recording-label">When Recorded Mail To:</div>
                <div class="recording-value">
                    {% if return_to %}
                        {% if return_to.get('name') %}{{ return_to.get('name') }}<br>{% endif %}
                        {% if return_to.get('company') %}{{ return_to.get('company') }}<br>{% endif %}
                        {% if return_to.get('address1') %}{{ return_to.get('address1') }}<br>{% endif %}
                        {% if return_to.get('address2') %}{{ return_to.get('address2') }}<br>{% endif %}
                        {% set city = return_to.get('city', '') %}
                        {% set state = return_to.get('state', '') %}
                        {% set zip = return_to.get('zip', '') %}
                        {% if city or state or zip %}
                            {{ city }}{% if city and (state or zip) %}, {% endif %}{{ state }} {{ zip }}
                        {% endif %}
                    {% else %}
                        _________________________________<br>
                        _________________________________<br>
                        _________________________________
                    {% endif %}
                </div>

                <div class="recording-label">Mail Tax Statements To:</div>
                <div class="recording-value">
                    {{ mail_tax_to or 'Same as above' }}
                </div>
            </div>

            <div class="recorder-box">
                <div class="recorder-label">
                    Space Above This Line<br>
                    For Recorder's Use Only
                </div>
            </div>
        </header>

        <!-- ================================================================
             REFERENCE LINE - APN, Title Order, Escrow
             ================================================================ -->

        <div class="reference-section">
            <div class="reference-row">
                <div class="reference-primary">
                    {% if apn %}
                    <span class="ref-label">APN:</span>
                    <span class="ref-value monospace">{{ apn }}</span>
                    {% endif %}
                </div>
                
                <div class="reference-secondary">
                    {% if title_order_no %}
                    <span class="ref-item">
                        <span class="ref-label">Title Order:</span>
                        <span class="ref-value">{{ title_order_no }}</span>
                    </span>
                    {% endif %}
                    
                    {% if escrow_no %}
                    <span class="ref-item">
                        <span class="ref-label">Escrow:</span>
                        <span class="ref-value">{{ escrow_no }}</span>
                    </span>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- ================================================================
             DOCUMENT TITLE
             ================================================================ -->

        <h1 class="deed-title">Grant Deed</h1>

        <!-- ================================================================
             DOCUMENTARY TRANSFER TAX
             ================================================================ -->

        <div class="dtt-section">
            <div class="dtt-header">Documentary Transfer Tax Declaration</div>
            
            <div class="dtt-amount-line">
                Documentary Transfer Tax is 
                <span class="dtt-amount-value">${{ dtt.get('amount', '___________') if dtt else '___________' }}</span>
            </div>

            <div class="dtt-options">
                <div class="dtt-option">
                    <span class="checkbox {% if not dtt or dtt.get('basis') == 'full' %}checked{% endif %}">
                        {% if not dtt or dtt.get('basis') == 'full' %}✓{% endif %}
                    </span>
                    <span>Computed on full value of property conveyed, <strong>OR</strong></span>
                </div>
                <div class="dtt-option">
                    <span class="checkbox {% if dtt and dtt.get('basis') == 'less_liens' %}checked{% endif %}">
                        {% if dtt and dtt.get('basis') == 'less_liens' %}✓{% endif %}
                    </span>
                    <span>Computed on full value less liens and encumbrances remaining at time of sale</span>
                </div>
            </div>

            <div class="dtt-area-row">
                <div class="dtt-option">
                    <span class="checkbox {% if not dtt or dtt.get('area_type') == 'unincorporated' %}checked{% endif %}">
                        {% if not dtt or dtt.get('area_type') == 'unincorporated' %}✓{% endif %}
                    </span>
                    <span>Unincorporated Area</span>
                </div>
                <div class="dtt-option">
                    <span class="checkbox {% if dtt and dtt.get('area_type') == 'city' %}checked{% endif %}">
                        {% if dtt and dtt.get('area_type') == 'city' %}✓{% endif %}
                    </span>
                    <span>City of <span class="city-field">{{ dtt.get('city_name', '') if dtt else '' }}</span></span>
                </div>
            </div>
        </div>

        <!-- ================================================================
             DEED BODY - Granting Clause
             ================================================================ -->

        <div class="deed-body">
            <p>FOR VALUABLE CONSIDERATION, receipt of which is hereby acknowledged,</p>

            <div class="party-block">
                <span class="party-name">{{ grantors_text or '_________________________________________________' }}</span>
                <span class="party-label">("Grantor")</span>
            </div>

            <p>hereby <span class="grant-verb">GRANT(S)</span> to</p>

            <div class="party-block">
                <span class="party-name">{{ grantees_text or '_________________________________________________' }}</span>
                {% if vesting %}
                <br><span class="party-label">{{ vesting }}</span>
                {% endif %}
                <span class="party-label">("Grantee")</span>
            </div>

            <p>
                the following described real property in the County of 
                <span class="county-name">{{ county or '_______________' }}</span>, 
                State of California:
            </p>
        </div>

        <!-- ================================================================
             LEGAL DESCRIPTION
             ================================================================ -->

        <div class="legal-section">
            {% if legal_description and legal_description|length > (exhibit_threshold or 600) %}
                <p class="legal-exhibit-ref">
                    See Exhibit "A" attached hereto and incorporated herein by this reference.
                </p>
            {% else %}
                <div class="legal-content">{{ legal_description or 'LEGAL DESCRIPTION TO BE INSERTED' }}</div>
            {% endif %}

            {% if apn %}
            <div class="apn-line">
                <span class="apn-label">Assessor's Parcel Number:</span>
                <span class="apn-value">{{ apn }}</span>
            </div>
            {% endif %}
        </div>

        <!-- ================================================================
             EXECUTION - Date & Signatures
             ================================================================ -->

        <div class="execution-section">
            <div class="execution-date">
                <span class="date-label">Dated:</span>
                <span class="date-value">{{ execution_date or now().strftime('%B %d, %Y') }}</span>
            </div>

            <div class="signatures">
                {% set signers = grantors_text.split(';') if grantors_text and ';' in grantors_text else [grantors_text] if grantors_text else [] %}
                
                {% for signer in signers %}
                    {% set signer_name = signer.strip() if signer else '' %}
                    {% if signer_name %}
                    <div class="signature-entry">
                        <div class="signature-line"></div>
                        <div class="signature-name">{{ signer_name }}</div>
                    </div>
                    {% endif %}
                {% endfor %}

                {% if not signers or signers|length == 0 %}
                <div class="signature-entry">
                    <div class="signature-line"></div>
                    <div class="signature-name">Grantor</div>
                </div>
                {% endif %}
            </div>
        </div>

        <!-- ================================================================
             QR VERIFICATION FOOTER
             ================================================================ -->

        {% if document_id %}
        <footer class="deed-footer">
            <div class="verification-row">
                <div class="qr-code">
                    {% if qr_code_data_url %}
                    <img src="{{ qr_code_data_url }}" alt="Verification QR Code" />
                    {% else %}
                    <!-- Placeholder for QR code -->
                    {% endif %}
                </div>
                <div class="verification-text">
                    <div class="verify-label">Verify this document:</div>
                    <div class="verify-url">deedpro.com/verify/{{ document_id }}</div>
                    <div class="verify-meta">
                        Generated by <span class="deedpro-brand">DeedPro</span> • {{ now().strftime('%B %d, %Y at %I:%M %p') }}
                    </div>
                </div>
            </div>
        </footer>
        {% endif %}

    </div>

    <!-- ====================================================================
         EXHIBIT A - Legal Description (if too long for main page)
         ==================================================================== -->

    {% if legal_description and legal_description|length > (exhibit_threshold or 600) %}
    <div class="page-break"></div>
    <div class="exhibit-page">
        <div class="exhibit-header">
            <div class="exhibit-title">Exhibit "A"</div>
            <div class="exhibit-subtitle">Legal Description</div>
            <div class="exhibit-reference">
                Attached to and made a part of Grant Deed dated {{ execution_date or now().strftime('%B %d, %Y') }}
            </div>
        </div>

        <div class="exhibit-content">{{ legal_description }}</div>

        {% if apn %}
        <div class="exhibit-apn">
            <span class="apn-label">Assessor's Parcel Number:</span>
            <span class="apn-value">{{ apn }}</span>
        </div>
        {% endif %}
    </div>
    {% endif %}

    <!-- ====================================================================
         PAGE 2 (or 3): CALIFORNIA ALL-PURPOSE ACKNOWLEDGMENT
         California Civil Code § 1189
         ==================================================================== -->
    {% set document_title = 'Grant Deed' %}
    {% include '_partials/notary_acknowledgment.jinja2' %}

</body>
</html>
